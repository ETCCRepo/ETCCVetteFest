# Copy this file to .ftp-credentials (same directory, gitignored) and fill in
# the real password. deploy/ftp-deploy.sh reads this file at runtime if the
# FTP_HOST/FTP_USER/FTP_PASS environment variables aren't already set, so a
# deploy needs no credentials typed on the command line at all — same pattern
# as ../BusinessWebExpress/.ftp-credentials.
#
# KEY=VALUE, one per line, no quotes.
FTP_HOST=ftp.etccapps.com
FTP_USER=u177039107.vettefest
FTP_PASS=
